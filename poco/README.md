# poco

The Poco F5 Pro: Termux services behind haproxy, and the phone itself.

Rows: overview (probes, cert, firing alerts), service graph, web traffic
(haproxy per backend), shoko, mealprep, host cpu and memory, host power and
thermal, storage (`/data`, USB SSD, FUSE latency).

Datasource uid `prom`. Metrics from node_exporter, blackbox_exporter,
haproxy_exporter, anubis, and `termux_*` from the monitoring repo's
`deploy/textfile.sh`.

Generated. Source: `sandravwc/monitoring`, `dashboards/gen.py` and
`dashboards/topology.py`. Do not edit this file by hand.
